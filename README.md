# PingPongDuo — Animated system overview

Interactive website based on page 1 of the supplied `Overview_fig.pptx`.

The site lives in `Demo_Video/`. It preserves the source figure and adds synchronized flow, player-role and task-phase animations. A separate Archify typed diagram captures the deployment relationships without changing the presentation artwork.

Implementation and verification details will be added with the website files.
